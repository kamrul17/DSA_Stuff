#include <bits/stdc++.h>
using namespace std;

int main()
{
    stack<int> s;
    s.push(1);
    s.push(2);
    s.push(3);
    s.push(4);
    stack<int> s1;
    s.push(11);
    s.push(22);
    s.push(33);
    s.push(44);

    // s.pop();
    // cout << s.empty() << endl;
    // while (!s.empty())
    // {
    //     cout << s.top() << endl;
    //     s.pop();
    // }
    // cout << s.size() << endl;
    // s.swap(s1);

    return 0;
}